---
import Layout from '../layouts/Layout.astro'
---

<Layout title="Stack Game 2D">
	<section>
		<h1>Bienvenido 🎮</h1>
		<div class="message">
			<p>Este es un juego simple de apilar bloques. Usa las flechas del teclado para mover el bloque y apílalos lo más alto posible.</p>
			<p>¡Buena suerte!</p>

			<a href="/game">
				<button class="game-button">
					<span>🕹️</span> JUGAR
				</button>
			</a>
		</div>
	</section>

	<footer class="credits-banner">
		<p>Desarrollado por <a href="https://porfolio-jade-eight.vercel.app/" target="_blank">Juan Cruz Martínez</a>, basado en proyecto de <a href="https://www.javascript100.dev/10-stack-game" target="_blank">@midudev</a>
		</p>
	</footer>
</Layout>

<style>
	* {
		margin: 0;
		padding: 0;
		box-sizing: border-box;
	}

	html, body {
		height: 100%;
	}

	section {
		width: 100vw;
		min-height: 100vh;
		display: flex;
		flex-direction: column;
		justify-content: center;
		align-items: center;
		background: linear-gradient(135deg, #140663, #166294);
		color: white;
	}

	.message {
		background: linear-gradient(145deg, #0a0a0a, #1a1a1a);
		border-radius: 10px;
		padding: 1.5rem;
		margin: 1.5rem;
		width: 80%;
		max-width: 500px;
		text-align: center;
		color: white;
		box-shadow: 
			0 10px 15px rgba(0, 0, 0, 0.5), /* Sombra grande para profundidad */
			inset 0 2px 5px rgba(255, 255, 255, 0.1); /* Sombra interior ligera para sensación de profundidad */
	}


	@keyframes slide {
		0% {
			transform: translateX(0);
		}
		50% {
			transform: translateX(10px); /* Desplazamiento a la derecha */
		}
		100% {
			transform: translateX(-10px); /* Desplazamiento a la izquierda */
		}
	}

	.game-button {
		background-color: #ff4757;
		color: white;
		border: none;
		padding: 1rem 2rem;
		font-size: 1.2rem;
		font-weight: bold;
		border-radius: 5px;
		cursor: pointer;
		transition: background-color 0.3s, box-shadow 0.3s;
		box-shadow: 0 8px 15px rgba(0, 0, 0, 0.2);
		margin-top: 2rem;
		position: relative;
		overflow: hidden;
		animation: slide 0.6s ease-in-out infinite alternate; /* Aplica la animación */
	}

	.game-button:hover {
		background-color: #e84118;
		box-shadow: 0 12px 20px rgba(0, 0, 0, 0.3);
	}

	.game-button:active {
		transform: scale(0.98);
		box-shadow: 0 4px 10px rgba(0, 0, 0, 0.2);
	}

	.game-button:focus {
		outline: none;
		box-shadow: 0 0 0 4px rgba(255, 71, 87, 0.5), 0 8px 15px rgba(0, 0, 0, 0.2); 
	}


	.credits-banner {
		position: absolute;
		bottom: 0;
		width: 100%;
		background-color: #333;
		color: white;
		text-align: center;
		padding: 1rem;
	}

	.credits-banner a {
		color: #ff4757;
		text-decoration: none;
		font-weight: bold;
	}

	.credits-banner a:hover {
		text-decoration: none;
		opacity: 0.7;
	}
	
</style>

